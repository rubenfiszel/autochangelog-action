name: autorelease

on:
  pull_request:
    types: [closed] 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: echo
        run: echo "$LABELS"
        env:
          LABELS: ${{ toJson(github.event.pull_request) }}
      - name: Pull request merged
        if: github.event.pull_request.merged == true && contains(toJSON(github.event.pull_request.labels.*.name), '"autorelease"')
        run: echo merged
